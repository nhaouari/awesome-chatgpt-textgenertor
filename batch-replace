filter "**/prompts/*"

replace-regex "required_values:\s*\n"
with ""

replace-regex "\n(?=author:)"
with ""

replace-regex "tags:\s*\n"
with ""

replace-regex "\n(?=version:)"
with ""

replace-regex "version: 0.0.1"
with "version: 0.0.2"

replace-regex "\n{{{selection}}}"
with "{{{selection}}}"